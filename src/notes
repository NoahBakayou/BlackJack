//        System.out.println("Hit or Stay?");
//        String str = input.nextLine();
//
////        boolean userTurn = false;
////        while (!userTurn) {
//            userHand = hit(userHand, shuffledDeck); // `hand` inside hit() will refer to userHand
//
//            System.out.println("Your hand: " + userHand);
//            System.out.println("Dealer's hand: " + dealerHand);
//
//            System.out.println("Hit or Stay?");
//            str = input.nextLine();
//
//
//            if (str.equals("stay")) {
//                int dealerScore;
//                int userScore;
//                dealerScore = checkScore(dealerHand, shuffledDeck);
//                userScore = checkScore(userHand, shuffledDeck);
//
//                while (dealerScore <= 17) {
//                    dealerHand = hit(dealerHand, shuffledDeck);
//                    userScore = checkScore(dealerHand, shuffledDeck);
//
//                    dealerScore = checkScore(dealerHand, shuffledDeck);
//
//                    if (dealerScore > 21) {
//                        System.out.println("bust");
//                        break;
//                    }
//
//                    if (dealerScore < userScore) {
//                        dealerHand = hit(dealerHand, shuffledDeck);
//
//                    }
//                    dealerScore = checkScore(dealerHand, shuffledDeck);
//
//
//                }
//            }
//        }
//
//        // userHand = hit(userHand, shuffledDeck); // `hand` inside hit() will refer to userHand
//        dealerHand = hit(dealerHand, shuffledDeck);

    }